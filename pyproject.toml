[project]
requires-python = ">=3.14"
name = "dotfiles"
version = "2.0.3"
dependencies = [
    "ansible-lint",
    "ansible",
    "mitogen",
    "yamllint",
    "vim-vint",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
