[project]
requires-python = ">=3.14"
name = "dotfiles"
version = "2.2.2"
dependencies = [
    "ansible-lint",
    "ansible",
    "yamllint",
    "vim-vint",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
