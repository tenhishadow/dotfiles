[project]
requires-python = ">=3.14"
name = "dotfiles"
version = "2.0.1"
dependencies = [
    "ansible-lint",
    "ansible",
    "mitogen",
    "yamllint",
    "vim-vint>=0.3.21",
    "setuptools<83",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
