[project]
requires-python = ">=3.14"
name = "dotfiles"
version = "1.6.8"
dependencies = [
    "ansible-lint",
    "ansible",
    "jmespath",
    "mitogen",
    "yamllint"
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
