[project]
name = "dotfiles"
version = "1.6.3"
dependencies = [
    "ansible-lint",
    "ansible",
    "jmespath",
    "vim-vint",
    "yamllint",
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
