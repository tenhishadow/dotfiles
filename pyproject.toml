[project]
requires-python = ">=3.14"
name = "dotfiles"
version = "2.0.0"
dependencies = [
    "ansible-lint",
    "ansible",
    "mitogen",
    "yamllint"
]

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
