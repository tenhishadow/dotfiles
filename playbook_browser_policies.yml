---
- name: Browser Policies | Apply managed policies
  hosts: this_host
  # Connection is local via inventory; the play escalates because every policy
  # file is written under /etc.
  become: true
  gather_facts: true
  roles:
    - role: browser_policies
      tags:
        - browser_policies
