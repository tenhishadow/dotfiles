#!/bin/sh
set -eu

data_home="${XDG_DATA_HOME:-${HOME}/.local/share}"
binary="${data_home}/codex-mcp/playwright/node_modules/.bin/playwright-mcp"
if [ ! -x "${binary}" ]; then
  printf '%s\n' 'Playwright MCP is not installed; run go-task codex:mcp:install in the dotfiles repository.' >&2
  exit 127
fi
exec "${binary}" "$@"
