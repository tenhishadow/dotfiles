name: "labeler"
on:
  - pull_request_target

jobs:
  labeler:
    # checkov:skip=CKV2_GHA_1: required
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
